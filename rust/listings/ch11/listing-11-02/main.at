// Listing 11-2: Test with assert! and custom failure message
fn greet(name String) String {
    f"Hello, ${name}!"
}

#[test]
fn greeting_contains_name() {
    let result = greet("Carol")
    assert(
        result.contains("Carol"),
        f"Greeting did not contain name, value was '${result}'"
    )
}
