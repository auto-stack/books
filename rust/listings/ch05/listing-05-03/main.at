// Listing 5-3: Rectangle area calculation using a type
type Rectangle {
    width int
    height int
}

fn area(rect Rectangle) int {
    rect.width * rect.height
}

fn main() {
    let rect1 = Rectangle(30, 50)
    print(f"The area of the rectangle is ${area(rect1)} square pixels.")
}
