// Listing 6-3: Enum with data in variants
enum IpAddr {
    V4 String
    V6 String
}

fn main() {
    let home = IpAddr.V4("127.0.0.1")
    let loopback = IpAddr.V6("::1")
    print("IP addresses created")
}
