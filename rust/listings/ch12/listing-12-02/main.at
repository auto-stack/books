// Listing 12-2: Reading a file
use.rust std::fs

fn main() ! {
    let args = env.args()
    if args.len() < 3 {
        panic("not enough arguments")
    }
    let query = args[1]
    let file_path = args[2]

    let contents = fs.read_to_string(file_path).!
    print(contents)
}
