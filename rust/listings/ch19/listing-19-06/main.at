// Listing 19-6: A function with parameters that destructure a tuple
fn print_coordinates(&(x, y) &(int, int)) {
    print(f"Current location: (${x}, ${y})")
}

fn main() {
    let point = (3, 5)
    print_coordinates(&point)
}
