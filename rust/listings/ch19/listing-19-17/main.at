// Listing 19-17: Ignoring parts of a tuple
fn main() {
    let numbers = (2, 4, 8, 16, 32)

    numbers is {
        (first, _, third, _, fifth) -> {
            print(f"Some numbers: ${first}, ${third}, ${fifth}")
        }
    }
}
