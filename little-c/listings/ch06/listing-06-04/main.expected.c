// Auto → C transpiled by a2c
#include <stdio.h>

int main(void) {
    printf("%s\n", "Pipes connect processes in C");
    printf("%s\n", "pipe() creates one-way data channel");
    printf("%s\n", "Auto uses message passing between tasks");

    const char* data = "Hello through pipe";
    printf("%s %s\n", "Data:", data);
    return 0;
}
