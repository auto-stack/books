// Auto → C transpiled by a2c
#include <stdio.h>

int main(void) {
    printf("%s\n", "Signals are C/POSIX asynchronous events");
    printf("%s\n", "SIGINT - interrupt (Ctrl+C)");
    printf("%s\n", "SIGTERM - termination request");
    printf("%s\n", "SIGSEGV - segmentation fault");
    printf("%s\n", "Auto does not expose raw signal handling");
    return 0;
}
