// Auto → C transpiled by a2c
#include <stdio.h>
#include <string.h>

int main(void) {
    printf("%s\n", "Stdlib functions wrap system calls");
    printf("%s\n", "Auto provides safe, high-level APIs");

    const char* msg = "Hello from Auto";
    printf("%s\n", msg);
    printf("%s %zu\n", "Length:", strlen(msg));
    return 0;
}
