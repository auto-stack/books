// Auto → C transpiled by a2c
#include <stdio.h>

int main(void) {
    printf("%s\n", "Process creation is a C/POSIX concept");
    printf("%s\n", "Auto uses task-based concurrency instead");
    printf("%s\n", "fork() creates a copy of the current process");
    printf("%s\n", "exec() replaces the process with a new program");
    return 0;
}
