type Point {
    x int
    y int
}

fn Point.modulus() float {
    float(self.x * self.x + self.y * self.y)
}

fn main() {
    let p Point = Point(3, 4)
    print("Point: (", p.x, ",", p.y, ")")
    print("Modulus:", p.modulus())
}
