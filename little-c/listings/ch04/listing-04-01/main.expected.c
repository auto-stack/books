// Auto → C transpiled by a2c
#include <stdio.h>

int main(void) {
    char* name = "Alice";
    int age = 30;
    float height = 1.68f;

    printf("%s %s\n", "Name:", name);
    printf("%s %d\n", "Age:", age);
    printf("%s %f\n", "Height:", height);
    printf("%s %s %s %d %s\n", "Formatted:", name, "is", age, "years old");
    return 0;
}
