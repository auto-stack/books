// Auto → C transpiled by a2c
#include <stdio.h>
#include <stdbool.h>

int main(void) {
    // Declarations with types
    const char* name = "Auto";
    int age = 25;
    double height = 1.75;
    bool active = true;

    printf("%s %s\n", "Name:", name);
    printf("%s %d\n", "Age:", age);
    printf("%s %f\n", "Height:", height);
    printf("%s %d\n", "Active:", active);
    return 0;
}
